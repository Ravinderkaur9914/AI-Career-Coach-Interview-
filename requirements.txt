# ── Core UI ──────────────────────────────────────────
streamlit>=1.32.0

# ── AI / LLM ─────────────────────────────────────────
openai>=1.30.0
langchain>=0.2.0
langchain-openai>=0.1.7
langchain-community>=0.2.0
python-dotenv>=1.0.1

# ── Vector database ───────────────────────────────────
faiss-cpu>=1.8.0

# ── PDF handling ──────────────────────────────────────
PyPDF2>=3.0.1
reportlab>=4.2.0

# ── Data / visualisation ──────────────────────────────
pandas>=2.1.0
numpy>=1.26.0
plotly>=5.22.0

# ── ML / embeddings ───────────────────────────────────
scikit-learn>=1.4.0
tiktoken>=0.7.0

# ── Optional: Voice (local only) ──────────────────────
# openai-whisper      # local Whisper model
# sounddevice         # microphone capture
# pyaudio             # alternative audio I/O

# ── Optional: Webcam confidence (local only) ──────────
# opencv-python>=4.9.0
# mediapipe>=0.10.0

# ── Optional: TTS playback (local only) ───────────────
# gtts>=2.5.0
# pygame>=2.5.0
